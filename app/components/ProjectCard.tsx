'use client';
import React from 'react'

const ProjectCard = () => {
  return (
    <div>
        <button onClick = {() => console.log("Click")}>Open Projects</button>
    </div>
  )
}

export default ProjectCard